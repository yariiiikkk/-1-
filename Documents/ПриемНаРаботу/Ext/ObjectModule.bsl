Процедура ПриЗаписи(Отказ)
    // Проверка на заполненность обязательных полей
    Если ПустаяСтрока(Сотрудник) Или ПустаяСтрока(Должность) Или ПустаяСтрока(Отдел) Тогда
        Отказ = Истина;
        Сообщить("Заполните все обязательные поля.");
        Возврат;
	КонецЕсли;
	

    // Создание новой записи в справочнике "Сотрудники"
    НовыйСотрудник = Справочники.Сотрудники.СоздатьЭлемент();
    НовыйСотрудник.Наименование = Сотрудник;// Предполагается, что поле называется "Наименование"
	НовыйСотрудник.Сотрудник = Сотрудник;
    НовыйСотрудник.Должность = Должность;
    НовыйСотрудник.Отдел = Отдел;
    НовыйСотрудник.Статус = Перечисления.СтатусыСотрудников.Работающий;

    // Сохраняем запись
    Попытка
        НовыйСотрудник.Записать();
        Сообщить("Сотрудник успешно добавлен.");
    Исключение
        Сообщить("Ошибка при сохранении сотрудника: " + ОписаниеОшибки());
        Отказ = Истина;
    КонецПопытки;
КонецПроцедуры


Процедура ОбработкаПроведения(Отказ, Режим)
	//{{__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!

	// регистр Работники 
	Движения.Работники.Записывать = Истина;
	Движение = Движения.Работники.Добавить();
	Движение.Период = Дата;
	Движение.Сотрудник = Сотрудник;
	Движение.Должность = Должность;
	Движение.Отдел = Отдел;

	//}}__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
КонецПроцедуры

